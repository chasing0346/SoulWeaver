# SoulWeaver 简洁使用教程

## 1. 导出微信聊天记录

使用 WeFlow 将微信聊天记录导出为 CSV 格式。建议只准备 `texts` 目录中的聊天 CSV；图片、语音、视频和其他媒体文件不是必需内容。

请确保你有权访问和使用这些聊天记录。SoulWeaver 开始分析前会要求你明确确认文件范围、排除项和目标人物。

## 2. 安装 SoulWeaver

下载并解压项目，将完整的 `soulweaver` 文件夹放入 Codex 的 Skills 目录：

```text
C:\Users\你的用户名\.codex\skills\soulweaver\
```

确认 `SKILL.md` 位于该文件夹的第一层：

```text
soulweaver\
├── SKILL.md
├── agents\
├── references\
└── scripts\
```

## 3. 蒸馏并生成人物 Skill

建议在 Codex 中选择 **GPT-5.6 Sol** 作为主模型，然后在聊天框输入：

```text
$soulweaver
```

根据提示依次提供：

1. 微信聊天记录的绝对路径；
2. 数据使用授权与允许读取的文件范围；
3. 需要蒸馏的目标人物；
4. 人物 Skill 的显示名称和调用名称。

确认后，SoulWeaver 会开始分块分析。默认情况下，主 Agent 负责规划、汇总和校验，并自动调用 **GPT-5.6 Luna** Worker 并行处理分块；不需要手动分别调用 Luna。

分析完成后，SoulWeaver 会先展示人物记忆、沟通风格、行为模式、示例和验证结果。检查无误后，回复确认创建并安装；未经确认不会安装最终人物 Skill。

## 4. 调用人物 Skill 对话

安装完成后，建议切换到 **GPT-5.5** 进行人物模拟对话。在聊天框中调用新生成的 Skill：

```text
$目标人物skill 宝宝早呀
```

也可以先单独输入 `$目标人物skill`，再发送后续消息。实际调用名称以生成和安装时确认的 slug 为准。

> 模型建议：聊天记录蒸馏阶段使用 **GPT-5.6 Sol + GPT-5.6 Luna**；调用人物 Skill 交流时优先使用 **GPT-5.5**。模型会影响生成风格，即使使用同一人物 Skill，不同模型的回复也可能有所差异。
